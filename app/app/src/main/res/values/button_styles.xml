<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="button_dark">
        <item name="android:textColor">@color/darkOnPrimary</item>
        <item name="android:backgroundTint">@color/darkPrimary</item>
        <item name="android:layout_marginLeft">@dimen/dim_30dp</item>
        <item name="android:layout_marginRight">@dimen/dim_30dp</item>
        <!-- Add more style attributes as needed -->
    </style>

    <style name="button_dark_contrast">
        <item name="android:textColor">@color/darkOnPrimary</item>
        <item name="android:backgroundTint">@color/darkPrimary</item>
        <item name="android:layout_marginLeft">@dimen/dim_30dp</item>
        <item name="android:layout_marginRight">@dimen/dim_30dp</item>
        <!-- Add more style attributes as needed -->
    </style>

    <style name="button_light">
        <item name="android:textColor">@color/lightOnPrimary</item>
        <item name="android:backgroundTint">@color/lightVariant</item>
        <item name="android:layout_marginLeft">@dimen/dim_30dp</item>
        <item name="android:layout_marginRight">@dimen/dim_30dp</item>
        <!-- Add more style attributes as needed -->
    </style>

</resources>